# Cargo's output for the embedded crate. `@rust_crate` generates the bindings
# module in memory, straight into the package's precompile image. The crate's
# Cargo.lock is committed: the facade is a cdylib built in place against it.
/deps/safe_ledger/target/
/Manifest.toml
