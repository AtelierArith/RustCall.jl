# Hot reload API

Hot reload of a [`@rust_crate`](crates.md) crate's library while it is in use.
The [Hot Reload](../hot_reload.md) guide walks through it with a runnable
example.

## Hot reload (`src/hot_reload.jl`)

```@autodocs
Modules = [RustCall]
Pages = [joinpath("src", "hot_reload.jl")]
```
